// Tailwind CSS v4 uses CSS-based configuration (@theme in index.css).
// This file is intentionally minimal — token configuration lives in src/index.css.
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
}
